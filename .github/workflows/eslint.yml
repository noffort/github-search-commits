name: Code Quality
on:
  pull_request:
    types:
      - opened
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run ESLint
        uses: stefanoeb/eslint-action@1.0.2
  